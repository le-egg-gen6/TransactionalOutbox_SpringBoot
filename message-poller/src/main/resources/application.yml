spring:
  application:
    name: message-poller

  datasource:
    message:
      order:
        jdbc-url:
        username:
        password:
        driver-class-name:
      payment:
        jdbc-url:
        username:
        password:
        driver-class-name:

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true

outbox:
  topic:
    order: test_1
    payment: test_2
